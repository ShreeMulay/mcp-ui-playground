{
  "name": "@mcp-ui-playground/playground",
  "version": "1.0.0",
  "description": "Minimal MCP-UI playground with mock UIResources",
  "type": "module",
  "scripts": {
    "dev": "bun --hot serve.ts",
    "build": "bun build ./app.tsx --outdir ./dist --minify",
    "preview": "bun run build && bun serve.ts",
    "static": "open index.html"
  },
  "dependencies": {
    "@mcp-ui/client": "^5.17.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "bun-types": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
