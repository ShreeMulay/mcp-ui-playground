{
  "name": "@mcp-ui-playground/server",
  "version": "1.0.0",
  "description": "MCP server with interactive UI tools using @mcp-ui/server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mcp-ui/server": "^5.16.2",
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
